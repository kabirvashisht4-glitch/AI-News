import React, { useState, useEffect } from "react";
export default function Body(props) {
    return (
        <main id="arena" style={{ backgroundColor: props.currentColor }}>
            <div className="sector1">{work}</div>
        </main>
    )
}
